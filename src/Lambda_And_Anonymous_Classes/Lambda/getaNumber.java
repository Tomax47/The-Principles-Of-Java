package Lambda_And_Anonymous_Classes.Lambda;

/**
 We aint need to use the @FunctionalInterface annotation, though it's better if we do so!
 Keep the code neat & perfect ;)
 */
@FunctionalInterface
public interface getaNumber {

    int generateANumber();
}
